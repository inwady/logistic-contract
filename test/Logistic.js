
contract("Logistic", function(accounts) {
    const role = {
        sender: accounts[0],
        receiver: accounts[1],
        trainCompany: accounts[2],
        airCompany: accounts[3]
    };

    it("base smoke test", function() {

    });
});